# Job 및 Step 마다 시작/종료시 특정 작업 수행하기 

## 개요

- 스프링 배치 프레임워크는 Job내부에 여러개의 Step을 수행할 수 있는 구조라는 것을 지금까지 아티클을 통해서 익숙해 졌을 것이다. 
- 그렇다면 Job 시작하고, 종료할 때 특정 작업(보통 처리된 통계 내역등을 확인하여 리포트를 보내야하는 경우)이 있는 경우라면 어떻게 할 수 있을까?
- 혹은 Step 을 시작하고, 종료할 때 특정 로깅을 해야한다면 어떻게 할 수 있을까? 
- 이번 아티클에서 스프링 배치는 Listener 를 이용할 수 있다.

## Job Listener 이용하기

- JobListener은 Job이 시작/종료 할때마다 특정 이벤트를 수행하고 싶은경우 사용된다. 
- 아래와 같이 Listener를 생성해볼 것이다. 

### JobListener 등록하기 

```java
package com.schooldevops.springbatch.batchsample.listener;

import lombok.extern.slf4j.Slf4j;
import org.springframework.batch.core.JobExecution;
import org.springframework.batch.core.JobExecutionListener;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Slf4j
@Configuration
public class JobListenerConfig {

    @Bean
    public JobExecutionListener jobExecutionListener() {
        return new JobExecutionListener() {
            @Override
            public void beforeJob(JobExecution jobExecution) {
                log.info(" >>>>>> Before job: Job {} is starting...", jobExecution.getJobInstance().getJobName());
            }

            @Override
            public void afterJob(JobExecution jobExecution) {
                log.info(" >>>>>> After job: Job {} is finished.", jobExecution.getJobInstance().getJobName());
            }
        };
    }
}

```

- JobExecutionListener는 내부적으로 beforeJob, afterJob 이 메소드가 있다. 
- beforeJob은 잡이 실행되기 전에 수행이 된다. 
- afterJob은 잡이 종료될때 수행이 된다. 

### JobListener 사용하기

```java
    @Bean
    public Job nextStepJob(Step step01, Step step02, JobRepository jobRepository, JobExecutionListener jobExecutionListener) {
        return new JobBuilder(NEXT_STEP_TASK, jobRepository)
                .incrementer(new RunIdIncrementer())
                .start(step01)
                .next(step02)
                .listener(jobExecutionListener)
                .build();
    }
```

- 위와 같이 JobBuilder로 생성할때 listener에 jobExecutionListener를 등록하면 된다. 

## Step Listener 이용하기 

- StepListener은 Step이 시작/종료 할때마다 특정 이벤트를 수행하고 싶은경우 사용된다. 
- 아래와 같이 Listener를 생성해볼 것이다. 

### StepListener 등록하기 

```java
package com.schooldevops.springbatch.batchsample.listener;

import lombok.extern.slf4j.Slf4j;
import org.springframework.batch.core.ExitStatus;
import org.springframework.batch.core.StepExecution;
import org.springframework.batch.core.StepExecutionListener;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Slf4j
@Configuration
public class StepListenerConfig {
    @Bean
    public StepExecutionListener stepExecutionListener() {
        return new StepExecutionListener() {
            @Override
            public void beforeStep(StepExecution stepExecution) {
                log.info("------ Before Step: Step {} is starting...", stepExecution.getStepName());

            }

            @Override
            public ExitStatus afterStep(StepExecution stepExecution) {
                log.info("------ After Step: Step {} is finished.", stepExecution.getStepName());
                return stepExecution.getExitStatus();
            }
        };
    }
}

```

- StepExecutionListener은 beforeStep, afterStep 2개의 메소드를 제공한다. 
- beforeStep 은 스텝이 시작될때 수행된다. 
- afterStep 은 스텝이 종료될때 수행된다. 

### StepListener 이용하기 

```java

    @Bean(name = "step01")
    public Step step01(JobRepository jobRepository, PlatformTransactionManager transactionManager, StepExecutionListener stepExecutionListener) {
        return new StepBuilder("step01", jobRepository)
                .tasklet(new Tasklet() {
                    @Override
                    public RepeatStatus execute(StepContribution contribution, ChunkContext chunkContext) throws Exception {
                        return RepeatStatus.FINISHED;
                    }
                }, transactionManager)
                .listener(stepExecutionListener)
                .build();
    }

    @Bean(name = "step02")
    public Step step02(JobRepository jobRepository, PlatformTransactionManager transactionManager, StepExecutionListener stepExecutionListener) {
        return new StepBuilder("step02", jobRepository)
                .tasklet(new Tasklet() {
                    @Override
                    public RepeatStatus execute(StepContribution contribution, ChunkContext chunkContext) throws Exception {
                        return RepeatStatus.FINISHED;
                    }
                }, transactionManager)
                .listener(stepExecutionListener)
                .build();
    }
```

- 위와 같이 stepBuilder을 정의할때 listener에 stepExecutionListener를 등록하면 된다. 

## 전체 소스 확인하기. 

- 배치 전체 코드는 다음과 같이 수행할 수 있다. 

```java
package com.schooldevops.springbatch.batchsample.jobs;

import lombok.extern.slf4j.Slf4j;
import org.springframework.batch.core.*;
import org.springframework.batch.core.job.builder.JobBuilder;
import org.springframework.batch.core.launch.support.RunIdIncrementer;
import org.springframework.batch.core.repository.JobRepository;
import org.springframework.batch.core.scope.context.ChunkContext;
import org.springframework.batch.core.step.builder.StepBuilder;
import org.springframework.batch.core.step.tasklet.Tasklet;
import org.springframework.batch.repeat.RepeatStatus;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.transaction.PlatformTransactionManager;

@Slf4j
@Configuration
public class NextStepTaskJobConfiguration {

    public static final String NEXT_STEP_TASK = "NEXT_STEP_TASK";

    @Autowired
    PlatformTransactionManager transactionManager;

    @Bean(name = "step01")
    public Step step01(JobRepository jobRepository, PlatformTransactionManager transactionManager, StepExecutionListener stepExecutionListener) {
        return new StepBuilder("step01", jobRepository)
                .tasklet(new Tasklet() {
                    @Override
                    public RepeatStatus execute(StepContribution contribution, ChunkContext chunkContext) throws Exception {
                        return RepeatStatus.FINISHED;
                    }
                }, transactionManager)
                .listener(stepExecutionListener)
                .build();
    }

    @Bean(name = "step02")
    public Step step02(JobRepository jobRepository, PlatformTransactionManager transactionManager, StepExecutionListener stepExecutionListener) {
        return new StepBuilder("step02", jobRepository)
                .tasklet(new Tasklet() {
                    @Override
                    public RepeatStatus execute(StepContribution contribution, ChunkContext chunkContext) throws Exception {
                        return RepeatStatus.FINISHED;
                    }
                }, transactionManager)
                .listener(stepExecutionListener)
                .build();
    }

    @Bean
    public Job nextStepJob(Step step01, Step step02, JobRepository jobRepository, JobExecutionListener jobExecutionListener) {
        return new JobBuilder(NEXT_STEP_TASK, jobRepository)
                .incrementer(new RunIdIncrementer())
                .start(step01)
                .next(step02)
                .listener(jobExecutionListener)
                .build();
    }
}

```

## 환경변수 알아보기. 

```yaml
# H2 DataBase?
spring:
  datasource:
    hikari:
      maximum-pool-size: 10
      driver-class-name: org.h2.Driver
      jdbc-url: jdbc:h2:mem:testdb
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  batch:
    job:
      name: NEXT_STEP_TASK

```

## 결과 확인하기 

```shell
2024-12-05T15:30:36.521+09:00  INFO 56208 --- [           main] com.zaxxer.hikari.HikariDataSource       : HikariPool-1 - Starting...
2024-12-05T15:30:36.581+09:00  INFO 56208 --- [           main] com.zaxxer.hikari.pool.HikariPool        : HikariPool-1 - Added connection conn0: url=jdbc:h2:mem:testdb user=SA
2024-12-05T15:30:36.581+09:00  INFO 56208 --- [           main] com.zaxxer.hikari.HikariDataSource       : HikariPool-1 - Start completed.
2024-12-05T15:30:36.668+09:00  INFO 56208 --- [           main] c.s.s.b.BatchSampleApplication           : Started BatchSampleApplication in 0.537 seconds (process running for 0.925)
2024-12-05T15:30:36.669+09:00  INFO 56208 --- [           main] o.s.b.a.b.JobLauncherApplicationRunner   : Running default command line with: []
2024-12-05T15:30:36.684+09:00  INFO 56208 --- [           main] o.s.b.c.l.support.SimpleJobLauncher      : Job: [SimpleJob: [name=NEXT_STEP_TASK]] launched with the following parameters: [{'run.id':'{value=1, type=class java.lang.Long, identifying=true}'}]
2024-12-05T15:30:36.690+09:00  INFO 56208 --- [           main] c.s.s.b.listener.JobListenerConfig       :  >>>>>> Before job: Job NEXT_STEP_TASK is starting...
2024-12-05T15:30:36.692+09:00  INFO 56208 --- [           main] o.s.batch.core.job.SimpleStepHandler     : Executing step: [step01]
2024-12-05T15:30:36.693+09:00  INFO 56208 --- [           main] c.s.s.b.listener.StepListenerConfig      : ------ Before Step: Step step01 is starting...
2024-12-05T15:30:36.696+09:00  INFO 56208 --- [           main] c.s.s.b.listener.StepListenerConfig      : ------ After Step: Step step01 is finished.
2024-12-05T15:30:36.696+09:00  INFO 56208 --- [           main] o.s.batch.core.step.AbstractStep         : Step: [step01] executed in 4ms
2024-12-05T15:30:36.698+09:00  INFO 56208 --- [           main] o.s.batch.core.job.SimpleStepHandler     : Executing step: [step02]
2024-12-05T15:30:36.698+09:00  INFO 56208 --- [           main] c.s.s.b.listener.StepListenerConfig      : ------ Before Step: Step step02 is starting...
2024-12-05T15:30:36.699+09:00  INFO 56208 --- [           main] c.s.s.b.listener.StepListenerConfig      : ------ After Step: Step step02 is finished.
2024-12-05T15:30:36.700+09:00  INFO 56208 --- [           main] o.s.batch.core.step.AbstractStep         : Step: [step02] executed in 1ms
2024-12-05T15:30:36.701+09:00  INFO 56208 --- [           main] c.s.s.b.listener.JobListenerConfig       :  >>>>>> After job: Job NEXT_STEP_TASK is finished.
2024-12-05T15:30:36.703+09:00  INFO 56208 --- [           main] o.s.b.c.l.support.SimpleJobLauncher      : Job: [SimpleJob: [name=NEXT_STEP_TASK]] completed with the following parameters: [{'run.id':'{value=1, type=class java.lang.Long, identifying=true}'}] and the following status: [COMPLETED] in 13ms
2024-12-05T15:30:36.708+09:00  INFO 56208 --- [ionShutdownHook] com.zaxxer.hikari.HikariDataSource       : HikariPool-1 - Shutdown initiated...
2024-12-05T15:30:36.712+09:00  INFO 56208 --- [ionShutdownHook] com.zaxxer.hikari.HikariDataSource       : HikariPool-1 - Shutdown completed.
Disconnected from the target VM, address: '127.0.0.1:62729', transport: 'socket'
```

1. 처음 시작할때 >>>>>> Before job: ... 이 수행되었다. 
2. Before Step: Step step01 이 출력 되었다.
3. After Step: Step step01 이 출력 되었다.
4. Before Step: Step step02 이 출력 되었다.
5. After Step: Step step02 이 출력 되었다.
6. 마지막으로 >>>>>> After job: Job... 이 수행되었다.

- 위에서 보는바와 같이 job시작 --> step01 시작 --> step01 종료 --> step02 시작 --> step03 종료 --> job 종료 순으로 수행이 되었다. 

## WrapUp

- Job의 시작/종료시에 특정 작업을 위해서는 JobExecutionListener를 이용한다. 
- Step의 시작/종료시에 특정 작업을 위해서는 StepExecutionListener를 이용한다. 
- 보통 JobExecutionListener은 최종 수행한 배치 Job의 통계정보를 리포팅 할 때 사용된다. 
- 보통 StepExecutionListener은 최종 수행한 스텝의 로깅 및 중간 집계 리포팅에 사용된다. 
- 참고로 Chunk도 Listener를 ChunkListener를 이용하여 처리할 수 있다. 
- ChunkListener은 beforeChunk, afterChunk, afterChunkError 이 있으며, 샘플 코드를 살펴보면 확인할 수 있다.